# LostBreadcrumbs Handoff - 2026-05-01

Last verified: 2026-05-01 23:50 KST.

## Current Status
- The recent work focused on stabilizing Unity scene hygiene and release-soak preflight reporting after repeated missing-script, TMP, and build-scene reference issues.
- `SampleScene` was previously repaired from GUID-less script references and duplicate core runtime components. A backup exists at `.codex-backups/SampleScene_20260427_235839.unity`.
- Current static checks show `Assets/Scenes/SampleScene.unity` has `guidless=0` and `duplicateCore=0` for the tracked core runtime components.
- MCP/Unity Editor access has been unreliable, so verification so far is filesystem/static validation. Unity compilation and menu execution remain unverified.
- This workspace is not currently a Git repository, so no staging/commit metadata is available from this folder.
- Latest maintenance pass expanded local static preflight scene hygiene checks from the hardcoded `SampleScene` scan to all enabled build scenes. Result: `pass=7`, `warn=3`, `fail=0`, `results=10`.

## Recently Completed Work
- Added build-scene script reference hygiene diagnostics to detect GUID-less `m_Script` references and duplicate core runtime components.
- Integrated the hygiene scan into `Fix Common Scene Issues`, auto-soak preflight, enter-play auto-fix summaries, and release-soak reports.
- Added `Run Auto Soak Preflight Only` so the project can refresh preflight diagnostics without entering Play Mode.
- Added `auto_soak_preflight_last_summary.txt` as a stable preflight summary artifact under `Logs/ReleaseSoak`.
- Added auto-soak trace session headers and retention trimming so long-running logs remain readable.
- Added a pre-PlayMode abort gate: auto soak stops before Play Mode if build-scene script hygiene fails.
- Added release-soak detailed report sections for preflight summary, status file, trace file path, last-write times, and trace tail.
- Refactored auto-soak preflight summary formatting and warning checks into shared helpers so the auto-run and preflight-only menus do not drift.
- Static code-hook check confirmed the preflight-only menu, build-scene hygiene menu path, preflight summary/warning helpers, and release-soak `Auto Preflight Trace` report output are still present.
- Added `Tools/RunStaticPreflight.ps1` so static scene hygiene, build-scene registration, conflict markers, preflight code hooks, and release-soak log artifacts can be checked without Unity/MCP.
- Ran the initial local static preflight and wrote `Logs/ReleaseSoak/local_static_preflight_last_summary.txt`; later freshness checks refined the current warning count.
- Tightened `Tools/RunStaticPreflight.ps1` so `ProjectSettings/EditorBuildSettings.asset` must contain `SampleScene` with `enabled: 1`, not only the scene path.
- Tightened `Tools/RunStaticPreflight.ps1` log artifact validation so old `auto_soak_flow_trace.log` and `auto_soak_flow_last_status.txt` entries become warnings instead of false-positive passes.
- Re-ran the local static preflight and wrote `Logs/ReleaseSoak/local_static_preflight_last_summary.txt` with `pass=7`, `warn=3`, `fail=0`.
- Expanded `Tools/RunStaticPreflight.ps1` conflict-marker scanning to project text assets under `Assets`, `ProjectSettings`, `Packages`, `Tools`, plus this handoff. Latest scan covered 10,313 files with 0 hits.
- Tightened `Tools/RunStaticPreflight.ps1` build-scene validation so `EditorBuildSettings.asset` `SampleScene` GUID must match `Assets/Scenes/SampleScene.unity.meta`.
- Added `Logs/ReleaseSoak/local_static_preflight_last_summary.json` as a machine-readable local static preflight artifact.
- Verified the JSON artifact parses with `ConvertFrom-Json`: `pass=7`, `warn=3`, `fail=0`, `results=10`.
- Added enabled-build-scene aggregate hygiene validation to `Tools/RunStaticPreflight.ps1`. Current enabled scene scan reports `enabledScenes=1`, `missing=0`, `guidless=0`, `duplicateCore=0`.

## Changed Files
- `Assets/_Project/Scripts/Editor/LostBreadcrumbsProjectSetup.cs`
  - New diagnostics/menu paths for build-scene hygiene and auto-soak preflight.
  - New preflight-only menu.
  - New summary writer and trace retention helpers.
  - New abort gate for failed build-scene script hygiene.
  - Shared `BuildAutoSoakPreflightSummary(...)` and `HasAutoSoakPreflightWarnings(...)` helpers for both auto-run and preflight-only paths.
- `Assets/_Project/Scripts/Managers/RegressionChecklistRunner.cs`
  - Release-soak detailed report now includes auto preflight summary/status/trace sections.
  - Report generation is guarded so missing/locked trace files do not block report writing.
- `Assets/Scenes/SampleScene.unity`
  - Previously repaired to remove GUID-less core script references and duplicate core runtime binding components.
- `.codex-backups/SampleScene_20260427_235839.unity`
  - Backup of scene before manual scene hygiene repair.
- `HANDOFF_2026-05-01.md`
  - This handoff/context packet.
- `Tools/RunStaticPreflight.ps1`
  - MCP-free static preflight script for repeatable local validation. Build scene validation now requires `SampleScene` to be registered, enabled, and GUID-matched with its `.meta`; enabled build scenes are scanned for GUID-less scripts and duplicate core components; log artifact checks warn on stale release-soak outputs; conflict-marker scanning covers project text assets.
- `Logs/ReleaseSoak/local_static_preflight_last_summary.txt`
  - Latest local static preflight output.
- `Logs/ReleaseSoak/local_static_preflight_last_summary.json`
  - Machine-readable local static preflight output for automation.

## Current Unresolved Issues
- Unity Editor compile verification has not been run in this environment.
- New menu items have not been executed inside Unity:
  - `LostBreadcrumbs/Gameplay/Regression/Run Auto Soak Preflight Only`
  - `LostBreadcrumbs/Gameplay/Regression/Log Auto Soak Trace Status`
  - `LostBreadcrumbs/Gameplay/Regression/Run Release Soak + Write Report File (Auto)`
  - `LostBreadcrumbs/Setup/Diagnostics/Project/Log Build Scene Script Reference Hygiene`
- `Logs/ReleaseSoak/auto_soak_preflight_last_summary.txt` does not exist yet because the new preflight menu has not been executed.
- Local static preflight is clean enough to proceed to Unity validation because `fail=0`, but currently reports `pass=7`, `warn=3`, `fail=0`.
- Current local static preflight warnings:
  - `Logs/ReleaseSoak/auto_soak_preflight_last_summary.txt` is missing because the Unity preflight menu has not been executed.
  - `auto_soak_flow_trace.log` is stale, last written on 2026-04-18.
  - `auto_soak_flow_last_status.txt` is stale, last written on 2026-04-18.
- Current local `Logs` entries are also stale Unity import/compiler logs, latest observed write time 2026-04-21.
- Runtime/gameplay validation is still needed for the earlier echo, feedback, HUD, decoy, risk-room, and map/monster movement work.
- Git status is unavailable because `D:\uni\gratelbread` is not a Git repository.

## Recommended Next Session Tasks
1. Run `powershell -ExecutionPolicy Bypass -File .\Tools\RunStaticPreflight.ps1` and confirm `fail=0`.
2. Open Unity and wait for compilation. Fix any compiler errors first.
3. Run `LostBreadcrumbs/Gameplay/Regression/Run Auto Soak Preflight Only`.
4. Run `LostBreadcrumbs/Gameplay/Regression/Log Auto Soak Trace Status` and confirm the preflight summary file is included.
5. If preflight is clean, run `LostBreadcrumbs/Gameplay/Regression/Run Release Soak + Write Report File (Auto)`.
6. Inspect the generated release-soak report and confirm the `Auto Preflight Trace` section includes:
   - preflight summary path and content
   - status path and last write time
   - trace path and last write time
   - trace tail
7. Playtest the gameplay loop focusing on:
   - fog-hidden monster echo readability
   - blocked-looking empty map spaces
   - monsters leaving map bounds
   - HUD alert noise level
   - save/load startup behavior

## Paste-Ready Context Packet For Next Session
```text
Project: D:\uni\gratelbread
Handoff last verified: 2026-05-01 23:50 KST.
Current focus: Unity release-readiness and scene hygiene stabilization.

Recent completed work:
- Added build scene script reference hygiene scan in LostBreadcrumbsProjectSetup.
- Added menu: LostBreadcrumbs/Setup/Diagnostics/Project/Log Build Scene Script Reference Hygiene.
- Integrated hygiene scan into Fix Common Scene Issues and auto-soak preflight.
- Added preflight-only menu: LostBreadcrumbs/Gameplay/Regression/Run Auto Soak Preflight Only.
- Added preflight summary artifact: Logs/ReleaseSoak/auto_soak_preflight_last_summary.txt.
- Added auto-soak trace session headers and 512KB retention preserving last 512 lines.
- Added auto-soak abort gate before Play Mode if build scene script hygiene fails.
- Updated RegressionChecklistRunner release-soak detailed report to include preflight summary/status/trace details.
- Refactored auto-soak preflight summary/warning logic into shared helpers in LostBreadcrumbsProjectSetup.
- Added MCP-free local static preflight script: Tools/RunStaticPreflight.ps1.
- Ran local static preflight and wrote Logs/ReleaseSoak/local_static_preflight_last_summary.txt.
- Tightened local static preflight build-scene validation to require SampleScene registered=True and enabled=True.
- Tightened local static preflight log artifact validation so stale auto-soak trace/status files are warnings.
- Expanded local static preflight conflict-marker scanning across project text assets; latest scan covered 10,313 files with 0 hits.
- Tightened local static preflight build-scene binding validation to require SampleScene registered=True enabled=True guidMatches=True.
- Added machine-readable local static preflight JSON output: Logs/ReleaseSoak/local_static_preflight_last_summary.json.
- Verified JSON parse: pass=7 warn=3 fail=0 results=10.
- Expanded static scene hygiene validation to all enabled build scenes: enabledScenes=1 missing=0 guidless=0 duplicateCore=0.

Changed files:
- Assets/_Project/Scripts/Editor/LostBreadcrumbsProjectSetup.cs
- Assets/_Project/Scripts/Managers/RegressionChecklistRunner.cs
- Assets/Scenes/SampleScene.unity
- .codex-backups/SampleScene_20260427_235839.unity
- HANDOFF_2026-05-01.md
- Tools/RunStaticPreflight.ps1
- Logs/ReleaseSoak/local_static_preflight_last_summary.txt
- Logs/ReleaseSoak/local_static_preflight_last_summary.json

Static status:
- SampleScene static scan: guidless m_Script refs = 0.
- Core runtime duplicate component scan = 0.
- ProjectSettings/EditorBuildSettings.asset includes Assets/Scenes/SampleScene.unity with registered=True enabled=True guidMatches=True.
- Static code-hook check confirms the preflight-only menu, build-scene hygiene menu path, preflight summary/warning helpers, and release-soak Auto Preflight Trace report output are present.
- Local static preflight result: pass=7, warn=3, fail=0. Build scene binding check reports registered=True enabled=True metaExists=True guidMatches=True. Enabled build scene hygiene reports enabledScenes=1 missing=0 guidless=0 duplicateCore=0. Conflict marker scan reports files=10313 hits=0. Warnings are the missing Unity-generated auto_soak_preflight_last_summary.txt and stale auto-soak trace/status files.
- JSON artifact parse status: local_static_preflight_last_summary.json loads successfully with results=10.

Known limitations:
- Unity compile and menu execution are not verified because MCP/Editor access was unreliable.
- Logs/ReleaseSoak/auto_soak_preflight_last_summary.txt will be created only after running the new preflight-only menu.
- The workspace is not a Git repository, so rely on filesystem diffs/backups unless version control is initialized elsewhere.

Next actions:
1. Run Tools/RunStaticPreflight.ps1 and confirm fail=0.
2. Let Unity compile; fix compiler errors if any.
3. Run Run Auto Soak Preflight Only.
4. Run Log Auto Soak Trace Status.
5. If clean, run Run Release Soak + Write Report File (Auto).
6. Validate report contains Auto Preflight Trace summary/status/trace details.
7. Playtest fog-hidden monster echo, blocked empty map spaces, monster map bounds, HUD noise, and save/load.
```
